[package]
authors = ["Scott Checko <schecko@sfu.ca>"]
edition = "2018"
name = "frontend"
publish = false
version = "0.1.0"

[dependencies]
common = { path = "../common" }
yew = "0.6.0"

[build-dependencies]
common = { path = "../common" }

